# ── media ──────────────────────────────────────────────────────────
# Videos are tracked via Git LFS (see .gitattributes) instead of being
# ignored, so delivery masters can be pushed to GitHub despite the
# 100 MB per-file limit on regular blobs.
assets/web/

# ── os / editor cruft ──────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
.vscode/
